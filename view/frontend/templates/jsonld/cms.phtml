,"name": "<?= /* @escapeNotVerified */  $block->escapeQuote($block->stripTags($block->getTitle())); ?>",
<?php /* @escapeNotVerified */  $content = $block->escapeQuote($block->stripTags($block->getContent())); ?>
<?php if ($content): ?>
"mainContentOfPage": {
    "text": "<?= $content; ?>"
}
<?php endif; ?>
<?php if ($block->getMetaDescription()): ?>
    ,"description": "<?= $block->escapeQuote(preg_replace('/\s+/', ' ', trim($block->stripTags($block->getMetaDescription())))); ?>"
<?php endif; ?>
<?php if ($block->getImage()): ?>
    ,"primaryImageOfPage": {
        "@type": "ImageObject",
        "url": "<?= $block->escapeQuote($block->getImage()); ?>"
    }
<?php endif; ?>
